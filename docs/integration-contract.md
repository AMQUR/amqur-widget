# Integration contract

Bootstrap `features` may include: chat, inventory, payments, vehicleCompare, savedVehicles, serviceAi, partsAi, proactiveEngagement, multilingual, leadCapture, handoff.
`proactive` and `locales` are optional bootstrap fields.
Chat response types unchanged; `provenance.disclaimer` is rendered when present.
