# Accessibility

- Dialog semantics, Escape close, focus return
- aria-live message region
- Compare table uses role=table
- Language toggle and dismissible proactive prompt
- Prefer reduced motion via existing theme CSS where present
