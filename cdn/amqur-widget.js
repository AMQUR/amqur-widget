/**
 * PLACEHOLDER — do not publish this file as the production widget.
 * Build the real IIFE with `npm run build` → dist/amqur-widget.iife.js
 * and publish that artifact (versioned) to your CDN.
 */
(function () {
  "use strict";
  if (typeof console !== "undefined") {
    console.error(
      "[AMQUR] cdn/amqur-widget.js is a stub. Deploy dist/amqur-widget.iife.js instead.",
    );
  }
})();
